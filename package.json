{
  "name": "reportready-root",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && node index.js"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "dotenv": "^17.4.2",
    "localtunnel": "^2.0.2",
    "resend": "^6.16.0"
  }
}
